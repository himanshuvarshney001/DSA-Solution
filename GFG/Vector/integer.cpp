#include<bits/stdc++.h>
using namespace std;
int main()
{
    double num;
    cout<<"Enter the real number=";
    cin>>num;
    if(floor(num)==num)
    cout<<"number is integer";
    else 
    cout<<"Number is not integer";
    return 0;
}