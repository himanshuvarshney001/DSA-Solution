#include<bits/stdc++.h>
using namespace std;
int main()
{
    vector<int> arr = {2, 41, 1, 0, 2};
    priority_queue<int> pq(arr.begin(),arr.end());
    arr.clear();
    while(pq.empty()==false)
    {
        
    }
}